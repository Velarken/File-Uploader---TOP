TODO LIST:

<- AUTH ->
** Signup **
    * XX implement express-validator for user account info at signup XX
        * XX username form field must be >= 3 and <= 16 XX 
        * XX firstName AND lastName form fields must be >=2 and <= 20 XX
        * XX email form field must be a vaild email XX
        * XX password AND confirmPassword form fields must be >= 8 XX
    * XX implement bcrypt password hashing for user password at signup XX
** Login **
    * check database for matching username
        * if no match found show 'username does not match records'
        * if username matches a database entry 
            * check entered password against un-hashed database password
    * create passport session for logged in user
        * add session to sessions table in database

<- FILE UPLOADING ->
* implement the multer middleware
* create account at supabase for file storage
    * read docs to familiarize with supabase
* implement file validation with multer's fileFIlter and limits
    * allow the following file types
        * PHOTOS - allow jpeg, jpg, png, gif, and webp
        * VIDEOS - allow mp4, wmv, mov
        * AUDIO - allow mp3 and wav
        * DOCUMENT - allow txt, word, and md
    * restrict all other file types, and provide an error message
    if user attempts to upload a restricted types
    * restrict any file that is greater than ___mb in size
    * if fileName already exists in current folder, ask that the new
    file be renamed, or remove old file and replace with new